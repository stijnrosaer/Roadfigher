//
// Created by stijn on 16/11/18.
//

#include "Game.h"

Game::Game() {
    //sf::RenderWindow window(sf::VideoMode(width, height), "Roadfighter");

    /*
    while (window.isOpen()) {
        sf::Event event;
        while (window.pollEvent(event)) {
            if (event.type == sf::Event::Closed)
                window.close();
        }

        window.clear();
        window.display();
    }
     */
}
